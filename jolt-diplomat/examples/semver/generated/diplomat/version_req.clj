(ns diplomat.version-req
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.parse-error :as parse-error]
  ))

(dr/defopaque VersionReq "sv_VersionReq_destroy_mv1")

(ffi/defcfn ^:private c-parse "jolt_sv_VersionReq_parse_mv1" [:string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-parse-result "jolt_sizeof_sv_VersionReq_parse_mv1_result" [] :int)
(defn parse [text]
  (let [sz (c-sizeof-parse-result) out (ffi/alloc sz)]
    (try
      (c-parse text (count text) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->VersionReq (ffi/read out :pointer 0) false)}
         {:ok? false :error (parse-error/->ParseError (ffi/read out :pointer 0) true)})
       "VersionReq/parse" parse-error/message)
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-matches "jolt_sv_VersionReq_matches_mv1" [:pointer :pointer] :int)
(defn matches [self version]
  (c-matches (:ptr self) (:ptr version))
)

(ffi/defcfn ^:private c-to-string "jolt_sv_VersionReq_to_string_mv1" [:pointer :pointer] :void)
(defn to-string [self]
  (dr/writeable-capture (fn [w__] (c-to-string (:ptr self) w__)))
)

