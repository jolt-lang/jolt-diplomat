(ns diplomat.date
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.calendar :as calendar]
            [diplomat.date-duration-unit :as date-duration-unit]
            [diplomat.date-overflow :as date-overflow]
            [diplomat.iso-date :as iso-date]
            [diplomat.weekday :as weekday]
  ))

(dr/defopaque Date "icu4x_Date_destroy_mv1")

(ffi/defcfn ^:private c-from-iso-in-calendar "jolt_icu4x_Date_from_iso_in_calendar_mv1" [:int :uint8 :uint8 :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-iso-in-calendar-result "jolt_sizeof_icu4x_Date_from_iso_in_calendar_mv1_result" [] :int)
(defn from-iso-in-calendar [iso-year iso-month iso-day calendar]
  (let [sz (c-sizeof-from-iso-in-calendar-result) out (ffi/alloc sz)]
    (try
      (c-from-iso-in-calendar iso-year iso-month iso-day (:ptr calendar) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Date (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Date/from-iso-in-calendar")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-codes-in-calendar "jolt_icu4x_Date_from_codes_in_calendar_mv1" [:string :size_t :int :string :size_t :uint8 :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-codes-in-calendar-result "jolt_sizeof_icu4x_Date_from_codes_in_calendar_mv1_result" [] :int)
(defn from-codes-in-calendar [era-code year month-code day calendar]
  (let [sz (c-sizeof-from-codes-in-calendar-result) out (ffi/alloc sz)]
    (try
      (c-from-codes-in-calendar era-code (count era-code) year month-code (count month-code) day (:ptr calendar) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Date (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Date/from-codes-in-calendar")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-rata-die "jolt_icu4x_Date_from_rata_die_mv1" [:int64 :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-rata-die-result "jolt_sizeof_icu4x_Date_from_rata_die_mv1_result" [] :int)
(defn from-rata-die [rd calendar]
  (let [sz (c-sizeof-from-rata-die-result) out (ffi/alloc sz)]
    (try
      (c-from-rata-die rd (:ptr calendar) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Date (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Date/from-rata-die")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-string "jolt_icu4x_Date_from_string_mv1" [:string :size_t :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-string-result "jolt_sizeof_icu4x_Date_from_string_mv1_result" [] :int)
(defn from-string [v calendar]
  (let [sz (c-sizeof-from-string-result) out (ffi/alloc sz)]
    (try
      (c-from-string v (count v) (:ptr calendar) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Date (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Date/from-string")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-to-calendar "jolt_icu4x_Date_to_calendar_mv1" [:pointer :pointer] :pointer)
(defn to-calendar [self calendar]
  (->Date (c-to-calendar (:ptr self) (:ptr calendar)) false)
)

(ffi/defcfn ^:private c-to-iso "icu4x_Date_to_iso_mv1" [:pointer] :pointer)
(defn to-iso [self] (iso-date/->IsoDate (c-to-iso (:ptr self)) false))

(ffi/defcfn ^:private c-to-rata-die "icu4x_Date_to_rata_die_mv1" [:pointer] :int64)
(defn to-rata-die [self] (c-to-rata-die (:ptr self)))

(ffi/defcfn ^:private c-day-of-year "icu4x_Date_day_of_year_mv1" [:pointer] :uint)
(defn day-of-year [self] (c-day-of-year (:ptr self)))

(ffi/defcfn ^:private c-day-of-month "icu4x_Date_day_of_month_mv1" [:pointer] :uint8)
(defn day-of-month [self] (c-day-of-month (:ptr self)))

(ffi/defcfn ^:private c-day-of-week "jolt_icu4x_Date_day_of_week_mv1" [:pointer] :int)
(defn day-of-week [self]
  (weekday/int->kw (c-day-of-week (:ptr self)))
)

(ffi/defcfn ^:private c-weekday "jolt_icu4x_Date_weekday_mv1" [:pointer] :int)
(defn weekday [self]
  (weekday/int->kw (c-weekday (:ptr self)))
)

(ffi/defcfn ^:private c-ordinal-month "icu4x_Date_ordinal_month_mv1" [:pointer] :uint8)
(defn ordinal-month [self] (c-ordinal-month (:ptr self)))

(ffi/defcfn ^:private c-month-code "jolt_icu4x_Date_month_code_mv1" [:pointer :pointer] :void)
(defn month-code [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-month-code (:ptr self) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-month-number "icu4x_Date_month_number_mv1" [:pointer] :uint8)
(defn month-number [self] (c-month-number (:ptr self)))

(ffi/defcfn ^:private c-month-is-leap "icu4x_Date_month_is_leap_mv1" [:pointer] :int)
(defn month-is-leap [self] (c-month-is-leap (:ptr self)))

(ffi/defcfn ^:private c-era-year-or-related-iso "icu4x_Date_era_year_or_related_iso_mv1" [:pointer] :int)
(defn era-year-or-related-iso [self] (c-era-year-or-related-iso (:ptr self)))

(ffi/defcfn ^:private c-extended-year "icu4x_Date_extended_year_mv1" [:pointer] :int)
(defn extended-year [self] (c-extended-year (:ptr self)))

(ffi/defcfn ^:private c-era "jolt_icu4x_Date_era_mv1" [:pointer :pointer] :void)
(defn era [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-era (:ptr self) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-months-in-year "icu4x_Date_months_in_year_mv1" [:pointer] :uint8)
(defn months-in-year [self] (c-months-in-year (:ptr self)))

(ffi/defcfn ^:private c-days-in-month "icu4x_Date_days_in_month_mv1" [:pointer] :uint8)
(defn days-in-month [self] (c-days-in-month (:ptr self)))

(ffi/defcfn ^:private c-days-in-year "icu4x_Date_days_in_year_mv1" [:pointer] :uint)
(defn days-in-year [self] (c-days-in-year (:ptr self)))

(ffi/defcfn ^:private c-is-in-leap-year "icu4x_Date_is_in_leap_year_mv1" [:pointer] :int)
(defn is-in-leap-year [self] (c-is-in-leap-year (:ptr self)))

(ffi/defcfn ^:private c-calendar "icu4x_Date_calendar_mv1" [:pointer] :pointer)
(defn calendar [self] (calendar/->Calendar (c-calendar (:ptr self)) false))

(ffi/defcfn ^:private c-try-add-with-options "jolt_icu4x_Date_try_add_with_options_mv1" [:pointer :int :uint :uint :uint :uint :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-try-add-with-options-result "jolt_sizeof_icu4x_Date_try_add_with_options_mv1_result" [] :int)
(defn try-add-with-options [self duration options]
  (let [sz (c-sizeof-try-add-with-options-result) out (ffi/alloc sz)]
    (try
      (c-try-add-with-options (:ptr self) (if (:is-negative duration) 1 0) (:years duration) (:months duration) (:weeks duration) (:days duration) (if (:overflow options) (date-overflow/kw->int (:overflow options)) 0) (if (nil? (:overflow options)) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Date (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Date/try-add-with-options")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-try-until-with-options "jolt_icu4x_Date_try_until_with_options_mv1" [:pointer :pointer :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-try-until-with-options-result "jolt_sizeof_icu4x_Date_try_until_with_options_mv1_result" [] :int)
(defn try-until-with-options [self other options]
  (let [sz (c-sizeof-try-until-with-options-result) out (ffi/alloc sz)]
    (try
      (c-try-until-with-options (:ptr self) (:ptr other) (if (:largest-unit options) (date-duration-unit/kw->int (:largest-unit options)) 0) (if (nil? (:largest-unit options)) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Date (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Date/try-until-with-options")
      (finally (ffi/free out))))
)

