(ns diplomat.data-provider
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
  ))

(dr/defopaque DataProvider "icu4x_DataProvider_destroy_mv1")

(ffi/defcfn ^:private c-from-fs "jolt_icu4x_DataProvider_from_fs_mv1" [:string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-fs-result "jolt_sizeof_icu4x_DataProvider_from_fs_mv1_result" [] :int)
(defn from-fs [path]
  (let [sz (c-sizeof-from-fs-result) out (ffi/alloc sz)]
    (try
      (c-from-fs path (count path) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DataProvider (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DataProvider/from-fs")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-owned-byte-slice "jolt_icu4x_DataProvider_from_owned_byte_slice_mv1" [:pointer :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-owned-byte-slice-result "jolt_sizeof_icu4x_DataProvider_from_owned_byte_slice_mv1_result" [] :int)
(defn from-owned-byte-slice [blob]
  (dr/with-primitive-buffer [blob-buf :uint8 blob]
    (let [sz (c-sizeof-from-owned-byte-slice-result) out (ffi/alloc sz)]
      (try
        (c-from-owned-byte-slice blob-buf (count blob) out)
        (dr/unwrap-result!
         (if (= 1 (ffi/read out :uint8 8))
           {:ok? true :value (->DataProvider (ffi/read out :pointer 0) false)}
           {:ok? false :error (ffi/read out :int 0)})
         "DataProvider/from-owned-byte-slice")
        (finally (ffi/free out))))
  )
)

(ffi/defcfn ^:private c-fork-by-marker "jolt_icu4x_DataProvider_fork_by_marker_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-fork-by-marker-result "jolt_sizeof_icu4x_DataProvider_fork_by_marker_mv1_result" [] :int)
(defn fork-by-marker [self other]
  (let [sz (c-sizeof-fork-by-marker-result) out (ffi/alloc sz)]
    (try
      (c-fork-by-marker (:ptr self) (:ptr other) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DataProvider (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DataProvider/fork-by-marker")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-fork-by-locale "jolt_icu4x_DataProvider_fork_by_locale_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-fork-by-locale-result "jolt_sizeof_icu4x_DataProvider_fork_by_locale_mv1_result" [] :int)
(defn fork-by-locale [self other]
  (let [sz (c-sizeof-fork-by-locale-result) out (ffi/alloc sz)]
    (try
      (c-fork-by-locale (:ptr self) (:ptr other) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DataProvider (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DataProvider/fork-by-locale")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-enable-locale-fallback-with "jolt_icu4x_DataProvider_enable_locale_fallback_with_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-enable-locale-fallback-with-result "jolt_sizeof_icu4x_DataProvider_enable_locale_fallback_with_mv1_result" [] :int)
(defn enable-locale-fallback-with [self fallbacker]
  (let [sz (c-sizeof-enable-locale-fallback-with-result) out (ffi/alloc sz)]
    (try
      (c-enable-locale-fallback-with (:ptr self) (:ptr fallbacker) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DataProvider (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DataProvider/enable-locale-fallback-with")
      (finally (ffi/free out))))
)

