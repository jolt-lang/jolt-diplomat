(ns diplomat.date-formatter
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.date-time-alignment :as date-time-alignment]
            [diplomat.date-time-length :as date-time-length]
            [diplomat.year-style :as year-style]
  ))

(dr/defopaque DateFormatter "icu4x_DateFormatter_destroy_mv1")

(ffi/defcfn ^:private c-create-d "jolt_icu4x_DateFormatter_create_d_mv1" [:pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-d-result "jolt_sizeof_icu4x_DateFormatter_create_d_mv1_result" [] :int)
(defn create-d [locale length alignment]
  (let [sz (c-sizeof-create-d-result) out (ffi/alloc sz)]
    (try
      (c-create-d (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-d")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-d-with-provider "jolt_icu4x_DateFormatter_create_d_with_provider_mv1" [:pointer :pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-d-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_d_with_provider_mv1_result" [] :int)
(defn create-d-with-provider [provider locale length alignment]
  (let [sz (c-sizeof-create-d-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-d-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-d-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-md "jolt_icu4x_DateFormatter_create_md_mv1" [:pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-md-result "jolt_sizeof_icu4x_DateFormatter_create_md_mv1_result" [] :int)
(defn create-md [locale length alignment]
  (let [sz (c-sizeof-create-md-result) out (ffi/alloc sz)]
    (try
      (c-create-md (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-md")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-md-with-provider "jolt_icu4x_DateFormatter_create_md_with_provider_mv1" [:pointer :pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-md-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_md_with_provider_mv1_result" [] :int)
(defn create-md-with-provider [provider locale length alignment]
  (let [sz (c-sizeof-create-md-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-md-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-md-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-ymd "jolt_icu4x_DateFormatter_create_ymd_mv1" [:pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-ymd-result "jolt_sizeof_icu4x_DateFormatter_create_ymd_mv1_result" [] :int)
(defn create-ymd [locale length alignment year-style]
  (let [sz (c-sizeof-create-ymd-result) out (ffi/alloc sz)]
    (try
      (c-create-ymd (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-ymd")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-ymd-with-provider "jolt_icu4x_DateFormatter_create_ymd_with_provider_mv1" [:pointer :pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-ymd-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_ymd_with_provider_mv1_result" [] :int)
(defn create-ymd-with-provider [provider locale length alignment year-style]
  (let [sz (c-sizeof-create-ymd-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-ymd-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-ymd-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-de "jolt_icu4x_DateFormatter_create_de_mv1" [:pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-de-result "jolt_sizeof_icu4x_DateFormatter_create_de_mv1_result" [] :int)
(defn create-de [locale length alignment]
  (let [sz (c-sizeof-create-de-result) out (ffi/alloc sz)]
    (try
      (c-create-de (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-de")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-de-with-provider "jolt_icu4x_DateFormatter_create_de_with_provider_mv1" [:pointer :pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-de-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_de_with_provider_mv1_result" [] :int)
(defn create-de-with-provider [provider locale length alignment]
  (let [sz (c-sizeof-create-de-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-de-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-de-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-mde "jolt_icu4x_DateFormatter_create_mde_mv1" [:pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-mde-result "jolt_sizeof_icu4x_DateFormatter_create_mde_mv1_result" [] :int)
(defn create-mde [locale length alignment]
  (let [sz (c-sizeof-create-mde-result) out (ffi/alloc sz)]
    (try
      (c-create-mde (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-mde")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-mde-with-provider "jolt_icu4x_DateFormatter_create_mde_with_provider_mv1" [:pointer :pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-mde-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_mde_with_provider_mv1_result" [] :int)
(defn create-mde-with-provider [provider locale length alignment]
  (let [sz (c-sizeof-create-mde-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-mde-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-mde-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-ymde "jolt_icu4x_DateFormatter_create_ymde_mv1" [:pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-ymde-result "jolt_sizeof_icu4x_DateFormatter_create_ymde_mv1_result" [] :int)
(defn create-ymde [locale length alignment year-style]
  (let [sz (c-sizeof-create-ymde-result) out (ffi/alloc sz)]
    (try
      (c-create-ymde (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-ymde")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-ymde-with-provider "jolt_icu4x_DateFormatter_create_ymde_with_provider_mv1" [:pointer :pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-ymde-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_ymde_with_provider_mv1_result" [] :int)
(defn create-ymde-with-provider [provider locale length alignment year-style]
  (let [sz (c-sizeof-create-ymde-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-ymde-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-ymde-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-e "jolt_icu4x_DateFormatter_create_e_mv1" [:pointer :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-e-result "jolt_sizeof_icu4x_DateFormatter_create_e_mv1_result" [] :int)
(defn create-e [locale length]
  (let [sz (c-sizeof-create-e-result) out (ffi/alloc sz)]
    (try
      (c-create-e (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-e")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-e-with-provider "jolt_icu4x_DateFormatter_create_e_with_provider_mv1" [:pointer :pointer :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-e-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_e_with_provider_mv1_result" [] :int)
(defn create-e-with-provider [provider locale length]
  (let [sz (c-sizeof-create-e-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-e-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-e-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-m "jolt_icu4x_DateFormatter_create_m_mv1" [:pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-m-result "jolt_sizeof_icu4x_DateFormatter_create_m_mv1_result" [] :int)
(defn create-m [locale length alignment]
  (let [sz (c-sizeof-create-m-result) out (ffi/alloc sz)]
    (try
      (c-create-m (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-m")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-m-with-provider "jolt_icu4x_DateFormatter_create_m_with_provider_mv1" [:pointer :pointer :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-m-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_m_with_provider_mv1_result" [] :int)
(defn create-m-with-provider [provider locale length alignment]
  (let [sz (c-sizeof-create-m-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-m-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-m-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-ym "jolt_icu4x_DateFormatter_create_ym_mv1" [:pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-ym-result "jolt_sizeof_icu4x_DateFormatter_create_ym_mv1_result" [] :int)
(defn create-ym [locale length alignment year-style]
  (let [sz (c-sizeof-create-ym-result) out (ffi/alloc sz)]
    (try
      (c-create-ym (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-ym")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-ym-with-provider "jolt_icu4x_DateFormatter_create_ym_with_provider_mv1" [:pointer :pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-ym-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_ym_with_provider_mv1_result" [] :int)
(defn create-ym-with-provider [provider locale length alignment year-style]
  (let [sz (c-sizeof-create-ym-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-ym-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-ym-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-y "jolt_icu4x_DateFormatter_create_y_mv1" [:pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-y-result "jolt_sizeof_icu4x_DateFormatter_create_y_mv1_result" [] :int)
(defn create-y [locale length alignment year-style]
  (let [sz (c-sizeof-create-y-result) out (ffi/alloc sz)]
    (try
      (c-create-y (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-y")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-y-with-provider "jolt_icu4x_DateFormatter_create_y_with_provider_mv1" [:pointer :pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-y-with-provider-result "jolt_sizeof_icu4x_DateFormatter_create_y_with_provider_mv1_result" [] :int)
(defn create-y-with-provider [provider locale length alignment year-style]
  (let [sz (c-sizeof-create-y-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-y-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) (if year-style (year-style/kw->int year-style) 0) (if (nil? year-style) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DateFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/create-y-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-format-iso "jolt_icu4x_DateFormatter_format_iso_mv1" [:pointer :pointer :pointer] :void)
(defn format-iso [self iso-date]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-format-iso (:ptr self) (:ptr iso-date) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-format-same-calendar "jolt_icu4x_DateFormatter_format_same_calendar_mv1" [:pointer :pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-format-same-calendar-result "jolt_sizeof_icu4x_DateFormatter_format_same_calendar_mv1_result" [] :int)
(defn format-same-calendar [self date]
  (let [sz (c-sizeof-format-same-calendar-result) out (ffi/alloc sz) buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-format-same-calendar (:ptr self) (:ptr date) w out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))}
         {:ok? false :error (ffi/read out :int 0)})
       "DateFormatter/format-same-calendar")
      (finally (ffi/free out) (ffi/free buf) (ffi/free w))))
)

