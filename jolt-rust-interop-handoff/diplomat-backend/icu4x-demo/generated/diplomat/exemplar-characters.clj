(ns diplomat.exemplar-characters
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
  ))

(dr/defopaque ExemplarCharacters "icu4x_ExemplarCharacters_destroy_mv1")

(ffi/defcfn ^:private c-contains-str "jolt_icu4x_ExemplarCharacters_contains_str_mv1" [:pointer :string :size_t] :int)
(defn contains-str [self s]
  (c-contains-str (:ptr self) s (count s))
)

(ffi/defcfn ^:private c-contains "icu4x_ExemplarCharacters_contains_mv1" [:pointer :uint] :int)
(defn contains [self cp] (c-contains (:ptr self) cp))

(ffi/defcfn ^:private c-create-main "jolt_icu4x_ExemplarCharacters_create_main_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-main-result "jolt_sizeof_icu4x_ExemplarCharacters_create_main_mv1_result" [] :int)
(defn create-main [locale]
  (let [sz (c-sizeof-create-main-result) out (ffi/alloc sz)]
    (try
      (c-create-main (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-main")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-main-with-provider "jolt_icu4x_ExemplarCharacters_create_main_with_provider_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-main-with-provider-result "jolt_sizeof_icu4x_ExemplarCharacters_create_main_with_provider_mv1_result" [] :int)
(defn create-main-with-provider [provider locale]
  (let [sz (c-sizeof-create-main-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-main-with-provider (:ptr provider) (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-main-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-auxiliary "jolt_icu4x_ExemplarCharacters_create_auxiliary_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-auxiliary-result "jolt_sizeof_icu4x_ExemplarCharacters_create_auxiliary_mv1_result" [] :int)
(defn create-auxiliary [locale]
  (let [sz (c-sizeof-create-auxiliary-result) out (ffi/alloc sz)]
    (try
      (c-create-auxiliary (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-auxiliary")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-auxiliary-with-provider "jolt_icu4x_ExemplarCharacters_create_auxiliary_with_provider_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-auxiliary-with-provider-result "jolt_sizeof_icu4x_ExemplarCharacters_create_auxiliary_with_provider_mv1_result" [] :int)
(defn create-auxiliary-with-provider [provider locale]
  (let [sz (c-sizeof-create-auxiliary-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-auxiliary-with-provider (:ptr provider) (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-auxiliary-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-punctuation "jolt_icu4x_ExemplarCharacters_create_punctuation_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-punctuation-result "jolt_sizeof_icu4x_ExemplarCharacters_create_punctuation_mv1_result" [] :int)
(defn create-punctuation [locale]
  (let [sz (c-sizeof-create-punctuation-result) out (ffi/alloc sz)]
    (try
      (c-create-punctuation (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-punctuation")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-punctuation-with-provider "jolt_icu4x_ExemplarCharacters_create_punctuation_with_provider_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-punctuation-with-provider-result "jolt_sizeof_icu4x_ExemplarCharacters_create_punctuation_with_provider_mv1_result" [] :int)
(defn create-punctuation-with-provider [provider locale]
  (let [sz (c-sizeof-create-punctuation-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-punctuation-with-provider (:ptr provider) (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-punctuation-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-numbers "jolt_icu4x_ExemplarCharacters_create_numbers_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-numbers-result "jolt_sizeof_icu4x_ExemplarCharacters_create_numbers_mv1_result" [] :int)
(defn create-numbers [locale]
  (let [sz (c-sizeof-create-numbers-result) out (ffi/alloc sz)]
    (try
      (c-create-numbers (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-numbers")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-numbers-with-provider "jolt_icu4x_ExemplarCharacters_create_numbers_with_provider_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-numbers-with-provider-result "jolt_sizeof_icu4x_ExemplarCharacters_create_numbers_with_provider_mv1_result" [] :int)
(defn create-numbers-with-provider [provider locale]
  (let [sz (c-sizeof-create-numbers-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-numbers-with-provider (:ptr provider) (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-numbers-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-index "jolt_icu4x_ExemplarCharacters_create_index_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-index-result "jolt_sizeof_icu4x_ExemplarCharacters_create_index_mv1_result" [] :int)
(defn create-index [locale]
  (let [sz (c-sizeof-create-index-result) out (ffi/alloc sz)]
    (try
      (c-create-index (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-index")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-index-with-provider "jolt_icu4x_ExemplarCharacters_create_index_with_provider_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-index-with-provider-result "jolt_sizeof_icu4x_ExemplarCharacters_create_index_with_provider_mv1_result" [] :int)
(defn create-index-with-provider [provider locale]
  (let [sz (c-sizeof-create-index-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-index-with-provider (:ptr provider) (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->ExemplarCharacters (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "ExemplarCharacters/create-index-with-provider")
      (finally (ffi/free out))))
)

