(ns diplomat.decimal-formatter
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.decimal-grouping-strategy :as decimal-grouping-strategy]
  ))

(dr/defopaque DecimalFormatter "icu4x_DecimalFormatter_destroy_mv1")

(ffi/defcfn ^:private c-create-with-grouping-strategy "jolt_icu4x_DecimalFormatter_create_with_grouping_strategy_mv1" [:pointer :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-with-grouping-strategy-result "jolt_sizeof_icu4x_DecimalFormatter_create_with_grouping_strategy_mv1_result" [] :int)
(defn create-with-grouping-strategy [locale grouping-strategy]
  (let [sz (c-sizeof-create-with-grouping-strategy-result) out (ffi/alloc sz)]
    (try
      (c-create-with-grouping-strategy (:ptr locale) (if grouping-strategy (decimal-grouping-strategy/kw->int grouping-strategy) 0) (if (nil? grouping-strategy) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DecimalFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DecimalFormatter/create-with-grouping-strategy")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-with-grouping-strategy-and-provider "jolt_icu4x_DecimalFormatter_create_with_grouping_strategy_and_provider_mv1" [:pointer :pointer :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-with-grouping-strategy-and-provider-result "jolt_sizeof_icu4x_DecimalFormatter_create_with_grouping_strategy_and_provider_mv1_result" [] :int)
(defn create-with-grouping-strategy-and-provider [provider locale grouping-strategy]
  (let [sz (c-sizeof-create-with-grouping-strategy-and-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-with-grouping-strategy-and-provider (:ptr provider) (:ptr locale) (if grouping-strategy (decimal-grouping-strategy/kw->int grouping-strategy) 0) (if (nil? grouping-strategy) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->DecimalFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "DecimalFormatter/create-with-grouping-strategy-and-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-with-manual-data "jolt_icu4x_DecimalFormatter_create_with_manual_data_mv1" [:string :size_t :string :size_t :string :size_t :string :size_t :string :size_t :string :size_t :uint8 :uint8 :uint8 :pointer :size_t :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-with-manual-data-result "jolt_sizeof_icu4x_DecimalFormatter_create_with_manual_data_mv1_result" [] :int)
(defn create-with-manual-data [plus-sign-prefix plus-sign-suffix minus-sign-prefix minus-sign-suffix decimal-separator grouping-separator primary-group-size secondary-group-size min-group-size digits grouping-strategy]
  (dr/with-primitive-buffer [digits-buf :uint digits]
    (let [sz (c-sizeof-create-with-manual-data-result) out (ffi/alloc sz)]
      (try
        (c-create-with-manual-data plus-sign-prefix (count plus-sign-prefix) plus-sign-suffix (count plus-sign-suffix) minus-sign-prefix (count minus-sign-prefix) minus-sign-suffix (count minus-sign-suffix) decimal-separator (count decimal-separator) grouping-separator (count grouping-separator) primary-group-size secondary-group-size min-group-size digits-buf (count digits) (if grouping-strategy (decimal-grouping-strategy/kw->int grouping-strategy) 0) (if (nil? grouping-strategy) 0 1) out)
        (dr/unwrap-result!
         (if (= 1 (ffi/read out :uint8 8))
           {:ok? true :value (->DecimalFormatter (ffi/read out :pointer 0) false)}
           {:ok? false :error (ffi/read out :int 0)})
         "DecimalFormatter/create-with-manual-data")
        (finally (ffi/free out))))
  )
)

(ffi/defcfn ^:private c-format "jolt_icu4x_DecimalFormatter_format_mv1" [:pointer :pointer :pointer] :void)
(defn format [self value]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-format (:ptr self) (:ptr value) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

