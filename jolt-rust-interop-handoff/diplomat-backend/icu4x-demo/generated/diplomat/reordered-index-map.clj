(ns diplomat.reordered-index-map
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
  ))

(dr/defopaque ReorderedIndexMap "icu4x_ReorderedIndexMap_destroy_mv1")

(ffi/defcfn ^:private c-as-slice "jolt_icu4x_ReorderedIndexMap_as_slice_mv1" [:pointer :pointer :pointer] :void)
(defn as-slice [self]
  (let [data-out (ffi/alloc 8) len-out (ffi/alloc 8)]
    (try
      (c-as-slice (:ptr self) data-out len-out)
      (let [ptr (ffi/read data-out :pointer 0) n (ffi/read len-out :size_t 0)]
        (ffi/read-array ptr :size_t n))
      (finally (ffi/free data-out) (ffi/free len-out))))
)

(ffi/defcfn ^:private c-len "icu4x_ReorderedIndexMap_len_mv1" [:pointer] :size_t)
(defn len [self] (c-len (:ptr self)))

(ffi/defcfn ^:private c-is-empty "icu4x_ReorderedIndexMap_is_empty_mv1" [:pointer] :int)
(defn is-empty [self] (c-is-empty (:ptr self)))

(ffi/defcfn ^:private c-get "icu4x_ReorderedIndexMap_get_mv1" [:pointer :size_t] :size_t)
(defn get [self index] (c-get (:ptr self) index))

