(ns diplomat.iana-parser-extended
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.time-zone-and-canonical-and-normalized-iterator :as time-zone-and-canonical-and-normalized-iterator]
            [diplomat.time-zone-and-canonical-iterator :as time-zone-and-canonical-iterator]
  ))

(dr/defopaque IanaParserExtended "icu4x_IanaParserExtended_destroy_mv1")

(ffi/defcfn ^:private c-create "icu4x_IanaParserExtended_create_mv1" [] :pointer)
(defn create [] (->IanaParserExtended (c-create ) false))

(ffi/defcfn ^:private c-create-with-provider "jolt_icu4x_IanaParserExtended_create_with_provider_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-with-provider-result "jolt_sizeof_icu4x_IanaParserExtended_create_with_provider_mv1_result" [] :int)
(defn create-with-provider [provider]
  (let [sz (c-sizeof-create-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-with-provider (:ptr provider) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->IanaParserExtended (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "IanaParserExtended/create-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-iter "icu4x_IanaParserExtended_iter_mv1" [:pointer] :pointer)
(defn iter [self] (time-zone-and-canonical-iterator/->TimeZoneAndCanonicalIterator (c-iter (:ptr self)) false))

(ffi/defcfn ^:private c-iter-all "icu4x_IanaParserExtended_iter_all_mv1" [:pointer] :pointer)
(defn iter-all [self] (time-zone-and-canonical-and-normalized-iterator/->TimeZoneAndCanonicalAndNormalizedIterator (c-iter-all (:ptr self)) false))

