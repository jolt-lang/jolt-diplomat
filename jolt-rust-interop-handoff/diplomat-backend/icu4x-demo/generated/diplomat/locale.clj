(ns diplomat.locale
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
  ))

(dr/defopaque Locale "icu4x_Locale_destroy_mv1")

(ffi/defcfn ^:private c-from-string "jolt_icu4x_Locale_from_string_mv1" [:string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-string-result "jolt_sizeof_icu4x_Locale_from_string_mv1_result" [] :int)
(defn from-string [name]
  (let [sz (c-sizeof-from-string-result) out (ffi/alloc sz)]
    (try
      (c-from-string name (count name) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Locale (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Locale/from-string")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-unknown "icu4x_Locale_unknown_mv1" [] :pointer)
(defn unknown [] (->Locale (c-unknown ) false))

(ffi/defcfn ^:private c-clone "icu4x_Locale_clone_mv1" [:pointer] :pointer)
(defn clone [self] (->Locale (c-clone (:ptr self)) false))

(ffi/defcfn ^:private c-basename "jolt_icu4x_Locale_basename_mv1" [:pointer :pointer] :void)
(defn basename [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-basename (:ptr self) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-get-unicode-extension "jolt_icu4x_Locale_get_unicode_extension_mv1" [:pointer :string :size_t :pointer] :int)
(defn get-unicode-extension [self s]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (let [ok (c-get-unicode-extension (:ptr self) s (count s) w)]
        (when (not= 0 ok) (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-language "jolt_icu4x_Locale_language_mv1" [:pointer :pointer] :void)
(defn language [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-language (:ptr self) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-set-language "jolt_icu4x_Locale_set_language_mv1" [:pointer :string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-set-language-result "jolt_sizeof_icu4x_Locale_set_language_mv1_result" [] :int)
(defn set-language [self s]
  (let [sz (c-sizeof-set-language-result) out (ffi/alloc sz)]
    (try
      (c-set-language (:ptr self) s (count s) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Locale (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Locale/set-language")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-region "jolt_icu4x_Locale_region_mv1" [:pointer :pointer] :int)
(defn region [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (let [ok (c-region (:ptr self) w)]
        (when (not= 0 ok) (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-set-region "jolt_icu4x_Locale_set_region_mv1" [:pointer :string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-set-region-result "jolt_sizeof_icu4x_Locale_set_region_mv1_result" [] :int)
(defn set-region [self s]
  (let [sz (c-sizeof-set-region-result) out (ffi/alloc sz)]
    (try
      (c-set-region (:ptr self) s (count s) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Locale (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Locale/set-region")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-script "jolt_icu4x_Locale_script_mv1" [:pointer :pointer] :int)
(defn script [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (let [ok (c-script (:ptr self) w)]
        (when (not= 0 ok) (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-set-script "jolt_icu4x_Locale_set_script_mv1" [:pointer :string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-set-script-result "jolt_sizeof_icu4x_Locale_set_script_mv1_result" [] :int)
(defn set-script [self s]
  (let [sz (c-sizeof-set-script-result) out (ffi/alloc sz)]
    (try
      (c-set-script (:ptr self) s (count s) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Locale (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Locale/set-script")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-variants "jolt_icu4x_Locale_variants_mv1" [:pointer :pointer] :void)
(defn variants [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-variants (:ptr self) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-variant-count "icu4x_Locale_variant_count_mv1" [:pointer] :size_t)
(defn variant-count [self] (c-variant-count (:ptr self)))

(ffi/defcfn ^:private c-variant-at "jolt_icu4x_Locale_variant_at_mv1" [:pointer :size_t :pointer] :int)
(defn variant-at [self index]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (let [ok (c-variant-at (:ptr self) index w)]
        (when (not= 0 ok) (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-has-variant "jolt_icu4x_Locale_has_variant_mv1" [:pointer :string :size_t] :int)
(defn has-variant [self s]
  (c-has-variant (:ptr self) s (count s))
)

(ffi/defcfn ^:private c-add-variant "jolt_icu4x_Locale_add_variant_mv1" [:pointer :string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-add-variant-result "jolt_sizeof_icu4x_Locale_add_variant_mv1_result" [] :int)
(defn add-variant [self s]
  (let [sz (c-sizeof-add-variant-result) out (ffi/alloc sz)]
    (try
      (c-add-variant (:ptr self) s (count s) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Locale (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Locale/add-variant")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-remove-variant "jolt_icu4x_Locale_remove_variant_mv1" [:pointer :string :size_t] :int)
(defn remove-variant [self s]
  (c-remove-variant (:ptr self) s (count s))
)

(ffi/defcfn ^:private c-clear-variants "icu4x_Locale_clear_variants_mv1" [:pointer] :void)
(defn clear-variants [self] (c-clear-variants (:ptr self)))

(ffi/defcfn ^:private c-normalize "jolt_icu4x_Locale_normalize_mv1" [:string :size_t :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-normalize-result "jolt_sizeof_icu4x_Locale_normalize_mv1_result" [] :int)
(defn normalize [s]
  (let [sz (c-sizeof-normalize-result) out (ffi/alloc sz) buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-normalize s (count s) w out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))}
         {:ok? false :error (ffi/read out :int 0)})
       "Locale/normalize")
      (finally (ffi/free out) (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-to-string "jolt_icu4x_Locale_to_string_mv1" [:pointer :pointer] :void)
(defn to-string [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-to-string (:ptr self) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-normalizing-eq "jolt_icu4x_Locale_normalizing_eq_mv1" [:pointer :string :size_t] :int)
(defn normalizing-eq [self other]
  (c-normalizing-eq (:ptr self) other (count other))
)

(ffi/defcfn ^:private c-compare-to-string "jolt_icu4x_Locale_compare_to_string_mv1" [:pointer :string :size_t] :int)
(defn compare-to-string [self other]
  (c-compare-to-string (:ptr self) other (count other))
)

(ffi/defcfn ^:private c-compare-to "jolt_icu4x_Locale_compare_to_mv1" [:pointer :pointer] :int)
(defn compare-to [self other]
  (c-compare-to (:ptr self) (:ptr other))
)

