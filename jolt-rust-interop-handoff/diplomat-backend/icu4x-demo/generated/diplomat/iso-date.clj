(ns diplomat.iso-date
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.date :as date]
            [diplomat.date-duration-unit :as date-duration-unit]
            [diplomat.date-overflow :as date-overflow]
            [diplomat.weekday :as weekday]
  ))

(dr/defopaque IsoDate "icu4x_IsoDate_destroy_mv1")

(ffi/defcfn ^:private c-create "jolt_icu4x_IsoDate_create_mv1" [:int :uint8 :uint8 :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-result "jolt_sizeof_icu4x_IsoDate_create_mv1_result" [] :int)
(defn create [year month day]
  (let [sz (c-sizeof-create-result) out (ffi/alloc sz)]
    (try
      (c-create year month day out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->IsoDate (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "IsoDate/create")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-rata-die "icu4x_IsoDate_from_rata_die_mv1" [:int64] :pointer)
(defn from-rata-die [rd] (->IsoDate (c-from-rata-die rd) false))

(ffi/defcfn ^:private c-from-string "jolt_icu4x_IsoDate_from_string_mv1" [:string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-string-result "jolt_sizeof_icu4x_IsoDate_from_string_mv1_result" [] :int)
(defn from-string [v]
  (let [sz (c-sizeof-from-string-result) out (ffi/alloc sz)]
    (try
      (c-from-string v (count v) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->IsoDate (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "IsoDate/from-string")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-to-calendar "jolt_icu4x_IsoDate_to_calendar_mv1" [:pointer :pointer] :pointer)
(defn to-calendar [self calendar]
  (date/->Date (c-to-calendar (:ptr self) (:ptr calendar)) false)
)

(ffi/defcfn ^:private c-to-any "icu4x_IsoDate_to_any_mv1" [:pointer] :pointer)
(defn to-any [self] (date/->Date (c-to-any (:ptr self)) false))

(ffi/defcfn ^:private c-to-rata-die "icu4x_IsoDate_to_rata_die_mv1" [:pointer] :int64)
(defn to-rata-die [self] (c-to-rata-die (:ptr self)))

(ffi/defcfn ^:private c-day-of-year "icu4x_IsoDate_day_of_year_mv1" [:pointer] :uint)
(defn day-of-year [self] (c-day-of-year (:ptr self)))

(ffi/defcfn ^:private c-day-of-month "icu4x_IsoDate_day_of_month_mv1" [:pointer] :uint8)
(defn day-of-month [self] (c-day-of-month (:ptr self)))

(ffi/defcfn ^:private c-day-of-week "jolt_icu4x_IsoDate_day_of_week_mv1" [:pointer] :int)
(defn day-of-week [self]
  (weekday/int->kw (c-day-of-week (:ptr self)))
)

(ffi/defcfn ^:private c-weekday "jolt_icu4x_IsoDate_weekday_mv1" [:pointer] :int)
(defn weekday [self]
  (weekday/int->kw (c-weekday (:ptr self)))
)

(ffi/defcfn ^:private c-month "icu4x_IsoDate_month_mv1" [:pointer] :uint8)
(defn month [self] (c-month (:ptr self)))

(ffi/defcfn ^:private c-year "icu4x_IsoDate_year_mv1" [:pointer] :int)
(defn year [self] (c-year (:ptr self)))

(ffi/defcfn ^:private c-is-in-leap-year "icu4x_IsoDate_is_in_leap_year_mv1" [:pointer] :int)
(defn is-in-leap-year [self] (c-is-in-leap-year (:ptr self)))

(ffi/defcfn ^:private c-months-in-year "icu4x_IsoDate_months_in_year_mv1" [:pointer] :uint8)
(defn months-in-year [self] (c-months-in-year (:ptr self)))

(ffi/defcfn ^:private c-days-in-month "icu4x_IsoDate_days_in_month_mv1" [:pointer] :uint8)
(defn days-in-month [self] (c-days-in-month (:ptr self)))

(ffi/defcfn ^:private c-days-in-year "icu4x_IsoDate_days_in_year_mv1" [:pointer] :uint)
(defn days-in-year [self] (c-days-in-year (:ptr self)))

(ffi/defcfn ^:private c-try-add-with-options "jolt_icu4x_IsoDate_try_add_with_options_mv1" [:pointer :int :uint :uint :uint :uint :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-try-add-with-options-result "jolt_sizeof_icu4x_IsoDate_try_add_with_options_mv1_result" [] :int)
(defn try-add-with-options [self duration options]
  (let [sz (c-sizeof-try-add-with-options-result) out (ffi/alloc sz)]
    (try
      (c-try-add-with-options (:ptr self) (if (:is-negative duration) 1 0) (:years duration) (:months duration) (:weeks duration) (:days duration) (if (:overflow options) (date-overflow/kw->int (:overflow options)) 0) (if (nil? (:overflow options)) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->IsoDate (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "IsoDate/try-add-with-options")
      (finally (ffi/free out))))
)

