(ns diplomat.bidi-paragraph
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.bidi-direction :as bidi-direction]
  ))

(dr/defopaque BidiParagraph "icu4x_BidiParagraph_destroy_mv1")

(ffi/defcfn ^:private c-set-paragraph-in-text "icu4x_BidiParagraph_set_paragraph_in_text_mv1" [:pointer :size_t] :int)
(defn set-paragraph-in-text [self n] (c-set-paragraph-in-text (:ptr self) n))

(ffi/defcfn ^:private c-direction "jolt_icu4x_BidiParagraph_direction_mv1" [:pointer] :int)
(defn direction [self]
  (bidi-direction/int->kw (c-direction (:ptr self)))
)

(ffi/defcfn ^:private c-size "icu4x_BidiParagraph_size_mv1" [:pointer] :size_t)
(defn size [self] (c-size (:ptr self)))

(ffi/defcfn ^:private c-range-start "icu4x_BidiParagraph_range_start_mv1" [:pointer] :size_t)
(defn range-start [self] (c-range-start (:ptr self)))

(ffi/defcfn ^:private c-range-end "icu4x_BidiParagraph_range_end_mv1" [:pointer] :size_t)
(defn range-end [self] (c-range-end (:ptr self)))

(ffi/defcfn ^:private c-reorder-line "jolt_icu4x_BidiParagraph_reorder_line_mv1" [:pointer :size_t :size_t :pointer] :int)
(defn reorder-line [self range-start range-end]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (let [ok (c-reorder-line (:ptr self) range-start range-end w)]
        (when (not= 0 ok) (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))))
      (finally (ffi/free buf) (ffi/free w))))
)

(ffi/defcfn ^:private c-level-at "icu4x_BidiParagraph_level_at_mv1" [:pointer :size_t] :uint8)
(defn level-at [self pos] (c-level-at (:ptr self) pos))

