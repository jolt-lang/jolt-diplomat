(ns diplomat.decimal
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.decimal-rounding-increment :as decimal-rounding-increment]
            [diplomat.decimal-sign :as decimal-sign]
            [diplomat.decimal-sign-display :as decimal-sign-display]
            [diplomat.decimal-signed-rounding-mode :as decimal-signed-rounding-mode]
  ))

(dr/defopaque Decimal "icu4x_Decimal_destroy_mv1")

(ffi/defcfn ^:private c-from-int32 "icu4x_Decimal_from_int32_mv1" [:int] :pointer)
(defn from-int32 [v] (->Decimal (c-from-int32 v) false))

(ffi/defcfn ^:private c-from-uint32 "icu4x_Decimal_from_uint32_mv1" [:uint] :pointer)
(defn from-uint32 [v] (->Decimal (c-from-uint32 v) false))

(ffi/defcfn ^:private c-from-int64 "icu4x_Decimal_from_int64_mv1" [:int64] :pointer)
(defn from-int64 [v] (->Decimal (c-from-int64 v) false))

(ffi/defcfn ^:private c-from-uint64 "icu4x_Decimal_from_uint64_mv1" [:uint64] :pointer)
(defn from-uint64 [v] (->Decimal (c-from-uint64 v) false))

(ffi/defcfn ^:private c-from-double-with-integer-precision "jolt_icu4x_Decimal_from_double_with_integer_precision_mv1" [:double :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-double-with-integer-precision-result "jolt_sizeof_icu4x_Decimal_from_double_with_integer_precision_mv1_result" [] :int)
(defn from-double-with-integer-precision [f]
  (let [sz (c-sizeof-from-double-with-integer-precision-result) out (ffi/alloc sz)]
    (try
      (c-from-double-with-integer-precision f out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Decimal (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Decimal/from-double-with-integer-precision")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-double-with-lower-magnitude "jolt_icu4x_Decimal_from_double_with_lower_magnitude_mv1" [:double :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-double-with-lower-magnitude-result "jolt_sizeof_icu4x_Decimal_from_double_with_lower_magnitude_mv1_result" [] :int)
(defn from-double-with-lower-magnitude [f magnitude]
  (let [sz (c-sizeof-from-double-with-lower-magnitude-result) out (ffi/alloc sz)]
    (try
      (c-from-double-with-lower-magnitude f magnitude out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Decimal (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Decimal/from-double-with-lower-magnitude")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-double-with-significant-digits "jolt_icu4x_Decimal_from_double_with_significant_digits_mv1" [:double :uint8 :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-double-with-significant-digits-result "jolt_sizeof_icu4x_Decimal_from_double_with_significant_digits_mv1_result" [] :int)
(defn from-double-with-significant-digits [f digits]
  (let [sz (c-sizeof-from-double-with-significant-digits-result) out (ffi/alloc sz)]
    (try
      (c-from-double-with-significant-digits f digits out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Decimal (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Decimal/from-double-with-significant-digits")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-double-with-round-trip-precision "jolt_icu4x_Decimal_from_double_with_round_trip_precision_mv1" [:double :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-double-with-round-trip-precision-result "jolt_sizeof_icu4x_Decimal_from_double_with_round_trip_precision_mv1_result" [] :int)
(defn from-double-with-round-trip-precision [f]
  (let [sz (c-sizeof-from-double-with-round-trip-precision-result) out (ffi/alloc sz)]
    (try
      (c-from-double-with-round-trip-precision f out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Decimal (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Decimal/from-double-with-round-trip-precision")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-string "jolt_icu4x_Decimal_from_string_mv1" [:string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-string-result "jolt_sizeof_icu4x_Decimal_from_string_mv1_result" [] :int)
(defn from-string [v]
  (let [sz (c-sizeof-from-string-result) out (ffi/alloc sz)]
    (try
      (c-from-string v (count v) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Decimal (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Decimal/from-string")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-digit-at "icu4x_Decimal_digit_at_mv1" [:pointer :int] :uint8)
(defn digit-at [self magnitude] (c-digit-at (:ptr self) magnitude))

(ffi/defcfn ^:private c-magnitude-start "icu4x_Decimal_magnitude_start_mv1" [:pointer] :int)
(defn magnitude-start [self] (c-magnitude-start (:ptr self)))

(ffi/defcfn ^:private c-magnitude-end "icu4x_Decimal_magnitude_end_mv1" [:pointer] :int)
(defn magnitude-end [self] (c-magnitude-end (:ptr self)))

(ffi/defcfn ^:private c-nonzero-magnitude-start "icu4x_Decimal_nonzero_magnitude_start_mv1" [:pointer] :int)
(defn nonzero-magnitude-start [self] (c-nonzero-magnitude-start (:ptr self)))

(ffi/defcfn ^:private c-nonzero-magnitude-end "icu4x_Decimal_nonzero_magnitude_end_mv1" [:pointer] :int)
(defn nonzero-magnitude-end [self] (c-nonzero-magnitude-end (:ptr self)))

(ffi/defcfn ^:private c-is-zero "icu4x_Decimal_is_zero_mv1" [:pointer] :int)
(defn is-zero [self] (c-is-zero (:ptr self)))

(ffi/defcfn ^:private c-multiply-pow10 "icu4x_Decimal_multiply_pow10_mv1" [:pointer :int] :void)
(defn multiply-pow10 [self power] (c-multiply-pow10 (:ptr self) power))

(ffi/defcfn ^:private c-sign "jolt_icu4x_Decimal_sign_mv1" [:pointer] :int)
(defn sign [self]
  (decimal-sign/int->kw (c-sign (:ptr self)))
)

(ffi/defcfn ^:private c-set-sign "jolt_icu4x_Decimal_set_sign_mv1" [:pointer :int] :void)
(defn set-sign [self sign]
  (c-set-sign (:ptr self) (decimal-sign/kw->int sign))
)

(ffi/defcfn ^:private c-apply-sign-display "jolt_icu4x_Decimal_apply_sign_display_mv1" [:pointer :int] :void)
(defn apply-sign-display [self sign-display]
  (c-apply-sign-display (:ptr self) (decimal-sign-display/kw->int sign-display))
)

(ffi/defcfn ^:private c-trim-start "icu4x_Decimal_trim_start_mv1" [:pointer] :void)
(defn trim-start [self] (c-trim-start (:ptr self)))

(ffi/defcfn ^:private c-trim-end "icu4x_Decimal_trim_end_mv1" [:pointer] :void)
(defn trim-end [self] (c-trim-end (:ptr self)))

(ffi/defcfn ^:private c-trim-end-if-integer "icu4x_Decimal_trim_end_if_integer_mv1" [:pointer] :void)
(defn trim-end-if-integer [self] (c-trim-end-if-integer (:ptr self)))

(ffi/defcfn ^:private c-pad-start "icu4x_Decimal_pad_start_mv1" [:pointer :int] :void)
(defn pad-start [self position] (c-pad-start (:ptr self) position))

(ffi/defcfn ^:private c-pad-end "icu4x_Decimal_pad_end_mv1" [:pointer :int] :void)
(defn pad-end [self position] (c-pad-end (:ptr self) position))

(ffi/defcfn ^:private c-set-max-position "icu4x_Decimal_set_max_position_mv1" [:pointer :int] :void)
(defn set-max-position [self position] (c-set-max-position (:ptr self) position))

(ffi/defcfn ^:private c-round "icu4x_Decimal_round_mv1" [:pointer :int] :void)
(defn round [self position] (c-round (:ptr self) position))

(ffi/defcfn ^:private c-ceil "icu4x_Decimal_ceil_mv1" [:pointer :int] :void)
(defn ceil [self position] (c-ceil (:ptr self) position))

(ffi/defcfn ^:private c-expand "icu4x_Decimal_expand_mv1" [:pointer :int] :void)
(defn expand [self position] (c-expand (:ptr self) position))

(ffi/defcfn ^:private c-floor "icu4x_Decimal_floor_mv1" [:pointer :int] :void)
(defn floor [self position] (c-floor (:ptr self) position))

(ffi/defcfn ^:private c-trunc "icu4x_Decimal_trunc_mv1" [:pointer :int] :void)
(defn trunc [self position] (c-trunc (:ptr self) position))

(ffi/defcfn ^:private c-round-with-mode "jolt_icu4x_Decimal_round_with_mode_mv1" [:pointer :int :int] :void)
(defn round-with-mode [self position mode]
  (c-round-with-mode (:ptr self) position (decimal-signed-rounding-mode/kw->int mode))
)

(ffi/defcfn ^:private c-round-with-mode-and-increment "jolt_icu4x_Decimal_round_with_mode_and_increment_mv1" [:pointer :int :int :int] :void)
(defn round-with-mode-and-increment [self position mode increment]
  (c-round-with-mode-and-increment (:ptr self) position (decimal-signed-rounding-mode/kw->int mode) (decimal-rounding-increment/kw->int increment))
)

(ffi/defcfn ^:private c-concatenate-end "jolt_icu4x_Decimal_concatenate_end_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-concatenate-end-result "jolt_sizeof_icu4x_Decimal_concatenate_end_mv1_result" [] :int)
(defn concatenate-end [self other]
  (let [sz (c-sizeof-concatenate-end-result) out (ffi/alloc sz)]
    (try
      (c-concatenate-end (:ptr self) (:ptr other) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Decimal (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Decimal/concatenate-end")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-to-string "jolt_icu4x_Decimal_to_string_mv1" [:pointer :pointer] :void)
(defn to-string [self]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-to-string (:ptr self) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

