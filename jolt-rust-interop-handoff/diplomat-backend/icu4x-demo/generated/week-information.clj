(ns diplomat.week-information
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.weekday :as weekday]
            [diplomat.weekday-set-iterator :as weekday-set-iterator]
  ))

(dr/defopaque WeekInformation "icu4x_WeekInformation_destroy_mv1")

(ffi/defcfn ^:private c-create "jolt_icu4x_WeekInformation_create_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-result "jolt_sizeof_icu4x_WeekInformation_create_mv1_result" [] :int)
(defn create [locale]
  (let [sz (c-sizeof-create-result) out (ffi/alloc sz)]
    (try
      (c-create (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->WeekInformation (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "WeekInformation/create")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-with-provider "jolt_icu4x_WeekInformation_create_with_provider_mv1" [:pointer :pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-with-provider-result "jolt_sizeof_icu4x_WeekInformation_create_with_provider_mv1_result" [] :int)
(defn create-with-provider [provider locale]
  (let [sz (c-sizeof-create-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-with-provider (:ptr provider) (:ptr locale) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->WeekInformation (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "WeekInformation/create-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-first-weekday "jolt_icu4x_WeekInformation_first_weekday_mv1" [:pointer] :int)
(defn first-weekday [self]
  (weekday/int->kw (c-first-weekday (:ptr self)))
)

(ffi/defcfn ^:private c-is-weekend "jolt_icu4x_WeekInformation_is_weekend_mv1" [:pointer :int] :int)
(defn is-weekend [self day]
  (c-is-weekend (:ptr self) (weekday/kw->int day))
)

(ffi/defcfn ^:private c-weekend "icu4x_WeekInformation_weekend_mv1" [:pointer] :pointer)
(defn weekend [self] (weekday-set-iterator/->WeekdaySetIterator (c-weekend (:ptr self)) false))

