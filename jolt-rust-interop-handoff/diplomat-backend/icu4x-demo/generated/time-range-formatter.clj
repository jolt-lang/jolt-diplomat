(ns diplomat.time-range-formatter
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
            [diplomat.date-time-alignment :as date-time-alignment]
            [diplomat.date-time-length :as date-time-length]
            [diplomat.time-precision :as time-precision]
  ))

(dr/defopaque TimeRangeFormatter "icu4x_TimeRangeFormatter_destroy_mv1")

(ffi/defcfn ^:private c-create "jolt_icu4x_TimeRangeFormatter_create_mv1" [:pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-result "jolt_sizeof_icu4x_TimeRangeFormatter_create_mv1_result" [] :int)
(defn create [locale length time-precision alignment]
  (let [sz (c-sizeof-create-result) out (ffi/alloc sz)]
    (try
      (c-create (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if time-precision (time-precision/kw->int time-precision) 0) (if (nil? time-precision) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->TimeRangeFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "TimeRangeFormatter/create")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-with-provider "jolt_icu4x_TimeRangeFormatter_create_with_provider_mv1" [:pointer :pointer :int :int :int :int :int :int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-with-provider-result "jolt_sizeof_icu4x_TimeRangeFormatter_create_with_provider_mv1_result" [] :int)
(defn create-with-provider [provider locale length time-precision alignment]
  (let [sz (c-sizeof-create-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-with-provider (:ptr provider) (:ptr locale) (if length (date-time-length/kw->int length) 0) (if (nil? length) 0 1) (if time-precision (time-precision/kw->int time-precision) 0) (if (nil? time-precision) 0 1) (if alignment (date-time-alignment/kw->int alignment) 0) (if (nil? alignment) 0 1) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->TimeRangeFormatter (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "TimeRangeFormatter/create-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-format "jolt_icu4x_TimeRangeFormatter_format_mv1" [:pointer :pointer :pointer :pointer] :void)
(defn format [self start-time end-time]
  (let [buf (ffi/alloc 256) w (ffi/alloc dr/writeable-struct-size)]
    (try
      (dr/simple-write! buf 256 w)
      (c-format (:ptr self) (:ptr start-time) (:ptr end-time) w)
      (let [n (ffi/read w :size_t dr/O-len)] (ffi/read-bytes buf n))
      (finally (ffi/free buf) (ffi/free w))))
)

