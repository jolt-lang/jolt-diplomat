(ns diplomat.utc-offset
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
  ))

(dr/defopaque UtcOffset "icu4x_UtcOffset_destroy_mv1")

(ffi/defcfn ^:private c-from-seconds "jolt_icu4x_UtcOffset_from_seconds_mv1" [:int :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-seconds-result "jolt_sizeof_icu4x_UtcOffset_from_seconds_mv1_result" [] :int)
(defn from-seconds [seconds]
  (let [sz (c-sizeof-from-seconds-result) out (ffi/alloc sz)]
    (try
      (c-from-seconds seconds out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->UtcOffset (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "UtcOffset/from-seconds")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-string "jolt_icu4x_UtcOffset_from_string_mv1" [:string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-string-result "jolt_sizeof_icu4x_UtcOffset_from_string_mv1_result" [] :int)
(defn from-string [offset]
  (let [sz (c-sizeof-from-string-result) out (ffi/alloc sz)]
    (try
      (c-from-string offset (count offset) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->UtcOffset (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "UtcOffset/from-string")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-seconds "icu4x_UtcOffset_seconds_mv1" [:pointer] :int)
(defn seconds [self] (c-seconds (:ptr self)))

(ffi/defcfn ^:private c-is-non-negative "icu4x_UtcOffset_is_non_negative_mv1" [:pointer] :int)
(defn is-non-negative [self] (c-is-non-negative (:ptr self)))

(ffi/defcfn ^:private c-is-zero "icu4x_UtcOffset_is_zero_mv1" [:pointer] :int)
(defn is-zero [self] (c-is-zero (:ptr self)))

(ffi/defcfn ^:private c-hours-part "icu4x_UtcOffset_hours_part_mv1" [:pointer] :int)
(defn hours-part [self] (c-hours-part (:ptr self)))

(ffi/defcfn ^:private c-minutes-part "icu4x_UtcOffset_minutes_part_mv1" [:pointer] :uint)
(defn minutes-part [self] (c-minutes-part (:ptr self)))

(ffi/defcfn ^:private c-seconds-part "icu4x_UtcOffset_seconds_part_mv1" [:pointer] :uint)
(defn seconds-part [self] (c-seconds-part (:ptr self)))

