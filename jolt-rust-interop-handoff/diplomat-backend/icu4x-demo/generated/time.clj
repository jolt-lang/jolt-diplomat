(ns diplomat.time
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
  ))

(dr/defopaque Time "icu4x_Time_destroy_mv1")

(ffi/defcfn ^:private c-create "jolt_icu4x_Time_create_mv1" [:uint8 :uint8 :uint8 :uint :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-result "jolt_sizeof_icu4x_Time_create_mv1_result" [] :int)
(defn create [hour minute second subsecond]
  (let [sz (c-sizeof-create-result) out (ffi/alloc sz)]
    (try
      (c-create hour minute second subsecond out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Time (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Time/create")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-from-string "jolt_icu4x_Time_from_string_mv1" [:string :size_t :pointer] :void)
(ffi/defcfn ^:private c-sizeof-from-string-result "jolt_sizeof_icu4x_Time_from_string_mv1_result" [] :int)
(defn from-string [v]
  (let [sz (c-sizeof-from-string-result) out (ffi/alloc sz)]
    (try
      (c-from-string v (count v) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Time (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Time/from-string")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-start-of-day "jolt_icu4x_Time_start_of_day_mv1" [:pointer] :void)
(ffi/defcfn ^:private c-sizeof-start-of-day-result "jolt_sizeof_icu4x_Time_start_of_day_mv1_result" [] :int)
(defn start-of-day []
  (let [sz (c-sizeof-start-of-day-result) out (ffi/alloc sz)]
    (try
      (c-start-of-day out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Time (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Time/start-of-day")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-noon "jolt_icu4x_Time_noon_mv1" [:pointer] :void)
(ffi/defcfn ^:private c-sizeof-noon-result "jolt_sizeof_icu4x_Time_noon_mv1_result" [] :int)
(defn noon []
  (let [sz (c-sizeof-noon-result) out (ffi/alloc sz)]
    (try
      (c-noon out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->Time (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "Time/noon")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-hour "icu4x_Time_hour_mv1" [:pointer] :uint8)
(defn hour [self] (c-hour (:ptr self)))

(ffi/defcfn ^:private c-minute "icu4x_Time_minute_mv1" [:pointer] :uint8)
(defn minute [self] (c-minute (:ptr self)))

(ffi/defcfn ^:private c-second "icu4x_Time_second_mv1" [:pointer] :uint8)
(defn second [self] (c-second (:ptr self)))

(ffi/defcfn ^:private c-subsecond "icu4x_Time_subsecond_mv1" [:pointer] :uint)
(defn subsecond [self] (c-subsecond (:ptr self)))

