(ns diplomat.case-map-closer
  "GENERATED by jolt-diplomat-backend from the real Diplomat HIR.
  Do not hand-edit — see findings/milestone-5-findings.md."
  (:require [jolt.ffi :as ffi]
            [diplomat.runtime :as dr]
  ))

(dr/defopaque CaseMapCloser "icu4x_CaseMapCloser_destroy_mv1")

(ffi/defcfn ^:private c-create "jolt_icu4x_CaseMapCloser_create_mv1" [:pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-result "jolt_sizeof_icu4x_CaseMapCloser_create_mv1_result" [] :int)
(defn create []
  (let [sz (c-sizeof-create-result) out (ffi/alloc sz)]
    (try
      (c-create out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->CaseMapCloser (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "CaseMapCloser/create")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-create-with-provider "jolt_icu4x_CaseMapCloser_create_with_provider_mv1" [:pointer :pointer] :void)
(ffi/defcfn ^:private c-sizeof-create-with-provider-result "jolt_sizeof_icu4x_CaseMapCloser_create_with_provider_mv1_result" [] :int)
(defn create-with-provider [provider]
  (let [sz (c-sizeof-create-with-provider-result) out (ffi/alloc sz)]
    (try
      (c-create-with-provider (:ptr provider) out)
      (dr/unwrap-result!
       (if (= 1 (ffi/read out :uint8 8))
         {:ok? true :value (->CaseMapCloser (ffi/read out :pointer 0) false)}
         {:ok? false :error (ffi/read out :int 0)})
       "CaseMapCloser/create-with-provider")
      (finally (ffi/free out))))
)

(ffi/defcfn ^:private c-add-case-closure-to "jolt_icu4x_CaseMapCloser_add_case_closure_to_mv1" [:pointer :uint :pointer] :void)
(defn add-case-closure-to [self c builder]
  (c-add-case-closure-to (:ptr self) c (:ptr builder))
)

(ffi/defcfn ^:private c-add-string-case-closure-to "jolt_icu4x_CaseMapCloser_add_string_case_closure_to_mv1" [:pointer :string :size_t :pointer] :int)
(defn add-string-case-closure-to [self s builder]
  (c-add-string-case-closure-to (:ptr self) s (count s) (:ptr builder))
)

