[package]
name = "icu4x-dylib"
version = "0.1.0"
edition = "2021"

[lib]
name = "icu4x"
crate-type = ["cdylib"]

[dependencies]
icu_capi = { version = "2.3.0", features = ["compiled_data", "locale", "std", "logging", "simple_logger"] }
