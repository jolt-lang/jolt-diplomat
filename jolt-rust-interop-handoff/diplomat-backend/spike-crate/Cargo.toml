[package]
name = "diplomat-jolt-spike"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
diplomat = "0.10"
diplomat-runtime = "0.10"

[profile.release]
panic = "abort"   # never let a Rust panic unwind across the FFI boundary
